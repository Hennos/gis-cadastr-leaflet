import React from 'react';

import Button from '../Button';

function SearchSubmit(props) {
  return (
    <div className="blockSearch_buttonSearch">
      <Button className="blockSearch_button">ПОИСК</Button>
    </div>
  );
}

export default SearchSubmit;